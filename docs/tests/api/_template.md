# テスト仕様書 - API-XXX-00 エンドポイント名
<!-- 例: API-USER-02 ユーザープロフィール作成 -->

## 対象API

| 項目 | 内容 |
|---|---|
| ID | API-XXX-00 |
| メソッド | POST |
| パス | /api/v1/xxx |
| 認証 | 不要 / JWT必須 |
| 成功時レスポンス | 200 OK / 201 Created / 204 No Content |

<!--
メソッドの選択肢：GET / POST / PUT / PATCH / DELETE
-->

## テストケース一覧

### 認証系（001〜099）
<!-- 認証不要の場合は「このAPIは認証不要のため、該当するテストケースはない。」と記載したうえで、認証ヘッダーが無視されることを確認するケースを記載する -->

| テストケースID | テスト内容 | 期待するテスト結果 |
|---|---|---|
| TC-001 | Authorizationヘッダーなしで最小有効なリクエストを送信する | 401 / UNAUTHORIZED / 認証が必要です |
| TC-002 | 不正な形式のトークン（`Bearer invalid_token`）を付与してリクエストを送信する | 401 / UNAUTHORIZED / トークンが無効です |
| TC-003 | 期限切れのトークンを付与してリクエストを送信する | 401 / UNAUTHORIZED / トークンが無効です |

---

### リクエストチェック（クエリ）（100〜199）
<!-- クエリパラメータがない場合は「このAPIにはクエリパラメータが存在しないため、該当するテストケースはない。」と記載して削除 -->

| テストケースID | テスト内容 | 期待するテスト結果 |
|---|---|---|
| TC-101 | `page` に `0` を指定する | 422 / VALIDATION_ERROR / ○○ |
| TC-102 | `page` に数値以外（例: `"abc"`）を指定する | 422 / VALIDATION_ERROR / ○○ |

---

### リクエストチェック（パラム）（200〜299）
<!-- URLパスパラメータがない場合は「このAPIにはURLパスパラメータが存在しないため、該当するテストケースはない。」と記載して削除 -->

| テストケースID | テスト内容 | 期待するテスト結果 |
|---|---|---|
| TC-201 | `id` に UUID 形式でない文字列（例: `"abc"`）を指定する | 404 / NOT_FOUND / ○○が見つかりません |
| TC-202 | `id` に存在しない UUID を指定する | 404 / NOT_FOUND / ○○が見つかりません |

---

### リクエストチェック（ボディ）（300〜399）
<!-- GET・DELETE など body がない場合はこのセクションを削除 -->

#### fieldName（項目名）

| テストケースID | テスト内容 | 期待するテスト結果 |
|---|---|---|
| TC-301 | `fieldName` を省略する | 422 / VALIDATION_ERROR / ○○は必須項目です |
| TC-302 | `fieldName` に `null` を指定する | 422 / VALIDATION_ERROR |
| TC-303 | `fieldName` に空文字 `""` を指定する | 422 / VALIDATION_ERROR / ○○は必須項目です |

#### リクエストボディ全体

| テストケースID | テスト内容 | 期待するテスト結果 |
|---|---|---|
| TC-374 | リクエストボディを送信しない（空リクエスト） | 400 / BAD_REQUEST / リクエストの形式が正しくありません |
| TC-375 | Content-Typeが `application/json` でないリクエストを送信する | 400 / BAD_REQUEST / リクエストの形式が正しくありません |

---

### 分岐処理（400〜499）
<!-- 正常系のバリエーションや業務ルールに基づく分岐を確認する -->

| テストケースID | テスト内容 | 期待するテスト結果 |
|---|---|---|
| TC-401 | 必須項目のみを含む最小有効なリクエストを送信する（任意項目はすべて省略） | 200 / 201 / 204 |
| TC-402 | すべての項目（任意項目含む）を含む完全なリクエストを送信する | 200 / 201 / 204 |

---

### エラー（500〜599）
<!-- DB エラー・外部サービスエラーなどサーバー起因のエラーを確認する -->

| テストケースID | テスト内容 | 期待するテスト結果 |
|---|---|---|
| TC-501 | DB 接続失敗時にリクエストを送信する | 500 / INTERNAL_SERVER_ERROR（メッセージは実行環境依存のため検証しない） |
| TC-502 | `NODE_ENV=production` 時に 500 エラーが発生した場合、スタックトレースがレスポンスに含まれないことを確認する | 500 / INTERNAL_SERVER_ERROR / `stack` キーが存在しない |

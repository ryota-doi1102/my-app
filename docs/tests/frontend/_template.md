# テスト仕様書 - FE-XXX-00 画面名
<!-- 例: FE-USER-02 ユーザープロフィール作成画面 -->

## 対象

| 項目 | 内容 |
|---|---|
| ID | FE-XXX-00 |
| 画面 | 画面名 |
| パス | /xxx/yyy |
| スキーマ | `shared/schemas/xxx.ts` `xxxSchema` |
| フック | `frontend/src/pages/xxx/useXxxForm.ts` |
| テストコード（スキーマ） | `frontend/src/tests/schemas/xxx.test.ts` |
| テストコード（フック） | `frontend/src/tests/hooks/useXxxForm.test.ts` |

## FE-XXX-00（○○）との差分
<!-- 別仕様書の画面と比較して変わっている点のみ記載する。差分なし・独立した仕様書の場合はこのセクションを削除 -->

| 項目 | ○○ | ○○ |
|---|---|---|
| `fieldName` フィールド | 必須 | なし |

## テストケース一覧

### 必須チェック（001〜099）
<!-- 必須フィールドに対して「省略」「空文字」「null」を確認する -->

#### fieldName（項目名）

| テストケースID | テスト内容 | 期待するテスト結果 |
|---|---|---|
| TC-001 | `fieldName` を省略する | invalid / ○○は必須項目です |
| TC-002 | `fieldName` に空文字 `""` を指定する | invalid / ○○は必須項目です |
| TC-003 | `fieldName` に `null` を指定する | invalid（型エラー） |

---

### 形式・制約チェック（100〜499）
<!-- フォーマット・文字数・許可値などの制約を確認する -->

#### fieldName（項目名）

| テストケースID | テスト内容 | 期待するテスト結果 |
|---|---|---|
| TC-101 | `fieldName` に○○文字を超える文字列を指定する | invalid / ○○は○○文字以下で入力してください |
| TC-102 | `fieldName` に許可値以外の値（例: `"unknown"`）を指定する | invalid / ○○の形式が正しくありません |

---

### 正常系バリエーション（500〜599）
<!-- バリデーションを通過するパターンを確認する -->

| テストケースID | テスト内容 | 期待するテスト結果 |
|---|---|---|
| TC-501 | 必須項目のみを含む最小有効なデータを指定する | valid |
| TC-502 | すべての項目（任意項目含む）を含む完全なデータを指定する | valid |

---

### API 変換（600〜699）
<!-- フックの onSubmit が API に渡す引数を確認する -->

| テストケースID | テスト内容 | 期待するテスト結果 |
|---|---|---|
| TC-601 | 必須項目を入力して送信する | `xxxApi()` が正しい引数で呼ばれる |
| TC-602 | 任意項目を空文字のまま送信する | 任意フィールドが `null` で API に渡る |
| TC-603 | 送信成功後 | `navigate("○○")` が呼ばれる |

---

### API エラー処理（700〜799）
<!-- API がエラーを返した場合のフックの挙動を確認する -->

| テストケースID | テスト内容 | 期待するテスト結果 |
|---|---|---|
| TC-701 | API がエラー（例: `Error("○○エラー")`）を返す | `apiError` にエラーメッセージが設定される |
| TC-702 | API エラー後 | `isSubmitting` が `false` に戻る |

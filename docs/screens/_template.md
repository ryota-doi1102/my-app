# 画面名
<!-- 例: ユーザープロフィール作成画面 -->

画面の概要を1〜2文で記載する。

## 基本情報

| 項目 | 内容 |
|---|---|
| ID | SCR-XXX-00 |
| パス | /xxx/yyy |
| 認証 | 不要 / JWT必須 |
| 概要 | この画面の説明 |

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.tsx` | ページコンポーネント（UI） |
| `useXxxForm.ts` | フォームロジック（RHF + zod） |
<!-- 必要に応じて行を追加・削除する -->

## 機能一覧

| ID | 機能名 | 機能概要 |
|---|---|---|
| F-01 | 機能名 | この機能の概要を1文で記載する |
| F-02 | 機能名 | この機能の概要を1文で記載する |

---

### F-01 機能名

機能の詳細を記載する。

<!--
フォームを持つ機能には以下のサブセクションを使う（不要なものは削除）:
- #### 項目一覧
- #### バリデーション仕様
- #### 選択肢の取得元
- #### ボタン
- #### 処理フロー
-->

#### 項目一覧
<!-- フォームを持たない機能はこのセクションを削除 -->

| # | 項目名 | 入力形式 | データ形式 | バリデーション | 備考 |
|---|---|---|---|---|---|
| 1 | フィールド名 | テキスト | string | 必須 / 任意 | |

<!--
入力形式の選択肢：
  テキスト / パスワード / 日付 / セレクトボックス / ラジオボタン
  単一チェックボックス / 複数チェックボックス / ファイルアップロード
  テキストエリア / 表示のみ
-->

#### バリデーション仕様
<!-- フォームを持たない機能はこのセクションを削除 -->

- バリデーションライブラリ: zod + React Hook Form
- バリデーションタイミング: 保存ボタン押下時（送信時のみ）
- エラーメッセージ表示位置: 各フィールドの直下
- スキーマ定義: `shared/schemas/xxx.ts` の `xxxSchema`

| フィールド | ルール | エラーメッセージ |
|---|---|---|
| フィールド名 | 必須項目 | フィールドは必須項目です |
| フィールド名 | ○○文字以下 | フィールドは○○文字以下で入力してください |

#### ボタン
<!-- ボタンがない機能はこのセクションを削除 -->

| ボタン名 | 動作 |
|---|---|
| 保存 | バリデーション後、保存処理を実行する |
| キャンセル | 一覧画面へ遷移する（入力がある場合は確認ダイアログを表示） |

#### 処理フロー
<!-- 複雑な処理がある機能のみ記載。なければこのセクションを削除 -->

1. 保存ボタン押下時にバリデーションを実行する
2. バリデーションエラーがある場合はエラーメッセージを各フィールドに表示し、処理を中断する
3. バリデーション通過後、API リクエストを送信する
4. リクエスト中は **全フィールドおよび全ボタンを disabled 状態** にして操作を不可とする
5. 保存完了後、○○画面（`/xxx`）へ遷移する
6. 遷移先で Snackbar を表示する（表示内容は画面遷移時の State で渡す）
   - 例: `{ severity: "success", message: "○○を保存しました" }`
7. 保存失敗時はエラーメッセージをページ上部に表示し、disabled を解除して再入力を促す

---

### F-02 機能名

機能の詳細を記載する。

## 状態管理
<!-- 管理すべき状態がある画面のみ記載。なければこのセクションを削除 -->

| 状態 | 型 | 説明 |
|---|---|---|
| `stateName` | `string` | 状態の説明 |

## API
<!-- 使用するAPIがある画面のみ記載。なければこのセクションを削除 -->

| Method | Path | 説明 |
|---|---|---|
| GET | `/api/v1/xxx` | ○○を取得する |

## 遷移元・遷移先

| 種別 | 画面 | URL |
|---|---|---|
| 遷移元 | ○○画面 | `/xxx` |
| 遷移先（保存成功時） | ○○画面 | `/xxx` |
| 遷移先（キャンセル時） | ○○画面 | `/xxx` |

## 関連ファイル

| 種別 | パス |
|---|---|
| ページコンポーネント | `frontend/src/pages/xxx/index.tsx` |
| フォームロジック | `frontend/src/pages/xxx/useXxxForm.ts` |
| APIクライアント | `frontend/src/lib/api/xxx.ts` |
| バリデーションスキーマ | `shared/schemas/xxx.ts` |

## 関連ドキュメント

### API仕様書

| ID | 説明 | ファイル |
|---|---|---|
| API-XXX-00 | ○○ | `docs/api/API-XXX-00.md` |

### テスト仕様書

| ID | 説明 | ファイル |
|---|---|---|
| FE-XXX-00 | ○○ | `docs/tests/frontend/FE-XXX-00.md` |

### DB仕様書
<!-- 関連テーブルがある場合のみ記載。なければこのセクションを削除 -->

| ID | テーブル / 型 | 説明 | ファイル |
|---|---|---|---|
| DB-XXX-00 | table_name | テーブルの説明 | `docs/db/DB-XXX-00.md` |

## 備考
<!-- 特記事項があれば記載。なければこのセクションを削除 -->

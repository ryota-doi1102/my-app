# ユーザープロフィール作成画面

新規ユーザーのプロフィール情報を入力・保存する画面。

## URL

`/users/create`

## ファイル構成

| ファイル | 役割 |
|----------|------|
| `index.tsx` | ページコンポーネント（UI） |
| `useUserCreateForm.ts` | フォームロジック（RHF + zod） |

## 項目一覧

| # | 項目名 | 入力形式 | データ形式 | バリデーション | 備考 |
|---|--------|----------|------------|----------------|------|
| 1 | 氏名 | テキスト | string | 必須 | |
| 2 | 生年月日 | 日付 | string | 必須・18歳以上60歳未満 | YYYY-MM-DD 形式 |
| 3 | 年齢 | テキスト（表示のみ） | number | — | 生年月日から自動計算。編集不可 |
| 4 | 性別 | ラジオボタン | string | 必須 | 選択肢: 男性 / 女性 / その他 |
| 5 | プロフィール画像 | ファイルアップロード | File | 任意 | JPEG / PNG / WebP。最大 5MB |
| 6 | 電話番号 | テキスト | string | 任意 | ハイフンなし数字。例: 09012345678 |
| 7 | メールアドレス | テキスト | string | 必須・メール形式 | |
| 8 | パスワード | パスワード | string | 必須・8文字以上 | マスク表示 |
| 9 | 郵便番号 | テキスト | string | 任意 | 7桁数字（ハイフンなし）。例: 1234567 |
| 10 | 住所（都道府県） | セレクトボックス | string | 任意 | 47都道府県から選択 |
| 11 | 住所（市区町村） | テキスト | string | 任意 | |
| 12 | 住所（番地） | テキスト | string | 任意 | |
| 13 | 住所（建物名・部屋番号） | テキスト | string | 任意 | |
| 14 | 希望勤務形態 | 複数チェックボックス | string[] | 任意 | 選択肢: フルタイム / パートタイム / リモート / フリーランス |
| 15 | 資格 | 配列（テキスト） | string[] | 任意 | 「追加」ボタンで入力欄を増やせる。各行に削除ボタンあり |
| 16 | 職歴 | 配列（構造体） | `{ company: string; startMonth: string; endMonth: string \| null; role: string }[]` | 必須・1件以上 | 各エントリに「会社名 / 在籍期間（開始月・終了月） / 役職」を入力。「追加」ボタンで行を増やせる。各行に削除ボタンあり |
| 17 | 自己PR | テキストエリア | string | 任意 | 複数行入力可 |
| 18 | 利用規約・プライバシーポリシーへの同意 | 単一チェックボックス | boolean | 必須 | チェックなしの場合は保存不可 |

## 年齢の自動計算仕様

- 生年月日が入力されると即時に年齢を計算して表示する
- 今日の日付を基準に満年齢を算出する
- 生年月日が未入力の場合は空欄とする

## ボタン

| ボタン名 | 動作 |
|----------|------|
| 保存 | バリデーション後、保存処理を実行する |
| キャンセル | ユーザー一覧画面へ遷移する（入力がある場合は確認ダイアログを表示） |

## 保存処理の仕様

1. 保存ボタン押下時にバリデーションを実行する
2. バリデーションエラーがある場合はエラーメッセージを各フィールドに表示し、処理を中断する
3. バリデーション通過後、API リクエストを送信する
4. リクエスト中は **全フィールドおよび全ボタンを disabled 状態** にして操作を不可とする
5. 保存完了後、ユーザー一覧画面（`/users`）へ遷移する
6. 遷移先で Snackbar を表示する（表示内容は画面遷移時の State で渡す）
   - 例: `{ severity: "success", message: "プロフィールを保存しました" }`
7. 保存失敗時はエラーメッセージをページ上部に表示し、disabled を解除して再入力を促す

## キャンセル処理の仕様

- いずれかの項目に入力がある場合、確認ダイアログを表示する
  - ダイアログ内容: 「入力内容が失われますが、よろしいですか？」
  - 「OK」押下 → ユーザー一覧画面へ遷移
  - 「キャンセル」押下 → ダイアログを閉じて入力を継続
- 全項目が未入力の場合は確認なしにユーザー一覧画面へ遷移する

> **注意**: ブラウザの戻るボタン・リロード・タブ閉じ等のブラウザネイティブ操作には確認ダイアログを表示しない（`beforeunload` 等は使用しない）

## バリデーション仕様

- バリデーションライブラリ: zod + React Hook Form
- バリデーションタイミング: 保存ボタン押下時（送信時のみ）
- エラーメッセージ表示位置: 各フィールドの直下
- スキーマ定義: `shared/schemas/user.ts` の `userProfileCreateSchema`

| フィールド | ルール | エラーメッセージ |
|------------|--------|------------------|
| 氏名 | 必須項目 | 氏名を入力してください |
| 生年月日 | 必須項目 | 生年月日を入力してください |
| 生年月日 | 18歳以上のみ許可 | 18歳未満の方は登録できません |
| 生年月日 | 60歳未満のみ許可 | 60歳以上の方は登録できません |
| 性別 | 必須項目 | 性別を選択してください |
| プロフィール画像 | JPEG / PNG / WebP 形式のみ許可 | JPEG、PNG、WebP 形式のファイルを選択してください |
| プロフィール画像 | 5MB 以下のみ許可 | ファイルサイズは 5MB 以下にしてください |
| 電話番号 | 入力がある場合は10〜11桁の数字のみ許可 | 電話番号は10〜11桁の数字で入力してください |
| メールアドレス | 必須項目 | メールアドレスを入力してください |
| メールアドレス | RFC 準拠のメール形式のみ許可 | 有効なメールアドレスを入力してください |
| パスワード | 必須項目 | パスワードを入力してください |
| パスワード | 8文字以上のみ許可 | パスワードは8文字以上で入力してください |
| 郵便番号 | 入力がある場合は7桁の数字のみ許可 | 郵便番号は7桁の数字で入力してください |
| 資格 | 入力がある各行は1文字以上 | 資格名を入力してください |
| 職歴 | 1件以上必須 | 職歴を1件以上入力してください |
| 職歴（会社名） | 必須項目 | 会社名を入力してください |
| 職歴（在籍開始月） | 必須項目 | 在籍開始月を入力してください |
| 職歴（役職） | 必須項目 | 役職を入力してください |
| 利用規約への同意 | チェック必須 | 利用規約・プライバシーポリシーに同意してください |

## 遷移元・遷移先

| 種別 | 画面 | URL |
|------|------|-----|
| 遷移元 | ユーザー一覧画面 | `/users` |
| 遷移先（保存成功時） | ユーザー一覧画面 | `/users` |
| 遷移先（キャンセル時） | ユーザー一覧画面 | `/users` |

## State（画面遷移時に渡すデータ）

```ts
type NavigateState = {
  snackbar: {
    severity: 'success' | 'error' | 'info' | 'warning';
    message: string;
  };
};
```

## 関連ファイル

| 種別 | パス |
|------|------|
| ページコンポーネント | `frontend/src/pages/users/create/index.tsx` |
| バリデーションスキーマ | `shared/schemas/user.ts` |